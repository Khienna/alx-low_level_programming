#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strcpy - copies
 * @dest: to be copies to src
 * @src: to be copioed to dest
 * Return: void
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
