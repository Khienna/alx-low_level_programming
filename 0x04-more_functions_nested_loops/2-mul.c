#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: to be multiplied by b
 * @b: to be multiplied by a
 * Return: 0.
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	printf("%d\n", c);
	return (0);
}
