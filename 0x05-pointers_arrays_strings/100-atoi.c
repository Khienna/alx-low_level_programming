#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _atoi - changes character to int
 * @s: character to be changed
 * Return: int variable
 */
int _atoi(char *s)
{
	int a;

	a = atoi(s);
	return (a);
}
